# Configuration for Deploy to Cloudflare button
# https://deploy.workers.cloudflare.com

[deploy]
# Build command - builds the Astro site
build = "npm run build"

# Deploy command - wrangler deploy reads main and assets from wrangler.toml
# and deploys as a Workers project with static assets
deploy = "npx wrangler deploy"
